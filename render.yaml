services:
  - type: web
    name: test-case-generator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        value: ${GROQ_API_KEY}
